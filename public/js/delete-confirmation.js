// Sistema de eliminación directa - sin confirmación
document.addEventListener('DOMContentLoaded', function() {
    // Los formularios de eliminación se envían directamente
    // El toast se maneja desde el controlador
});
